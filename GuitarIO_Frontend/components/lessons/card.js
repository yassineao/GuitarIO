import React from 'react';

const Card = ({ content  }) => {
  return (
    <div id="cardi" class="brutalist-card">
    
      <div class="brutalist-card__alert">{content}</div>
    </div>

  
  );
};

export default Card;
